
To run the script do the following steps: 
	
	1. pip install -r requirements.txt

	2. python subdomain.py --help

	3. sudo apt-get install host

	4.  python subdomain.py example.com  wordlists.txt  -t 20